# Normalize line endings to LF in the repo and on checkout everywhere.
# Zig source disallows carriage returns, so LF is required, not just preferred.
* text=auto eol=lf

# Binaries — never normalize.
*.png binary
*.ico binary
